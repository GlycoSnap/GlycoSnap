services:
  model:
    build: ./Model
    container_name: glycosnap_flask
    volumes:
      - ./Model:/app
      - C:/GlycoSnap/credentials.json:/app/credentials.json:ro
      - C:/GlycoSnap/model:/tmp
    ports:
      - "5000:5000"
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/app/credentials.json
      - FLASK_APP=app.py  # Ensure this is set correctly
    command: ["flask", "run", "--host=0.0.0.0", "--port=5000"]
  flutter_app:
    build: 
      context: .
    container_name: glycosnap_flutter
    volumes:
      - .:/app  # Mount local code into the container
    ports:
      - "8080:8080"
    depends_on:
      - model
    environment:
      - FLASK_API_URL=http://model:5000
    command: ["flutter", "run", "--debug"]